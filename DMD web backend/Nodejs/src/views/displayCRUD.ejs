<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get CRUD</title>
</head>
<style>
    table,
    th,
    td {
        border: 1px solid black;
        border-collapse: collapse;
    }

    .btn-edit {
        outline: none;
        border: none;
        background: orange;
        height: 25px;
        width: 60px;
        border-radius: 3px;
        cursor: pointer;
    }

    .btn-delete {
        outline: none;
        border: none;
        background: #ff0023;
        height: 25px;
        width: 60px;
        border-radius: 3px;
        cursor: pointer;
    }
</style>

<body>

    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>Ho</th>
            <th>Ten</th>
            <th>DiaChi</th>
        </tr>
        <tr>
            <% for(let i=0; i < dataTable.length; i++) { %>
        <tr>
            <td>
                <%= dataTable[i].email %>
            </td>
            <td>
                <%= dataTable[i].Ho %>
            </td>
            <td>
                <%= dataTable[i].Ten %>
            </td>
            <td>
                <%= dataTable[i].DiaChi %>
            </td>
            <td>
                <a href="/edit-crud?id=<%= dataTable[i].id %>" class="btn-edit" type="button">Sửa</a>
                <a href="/delete-crud?id=<%= dataTable[i].id %>" class="btn-delete" type="button">Xóa</a>
            </td>
        </tr>
        <% } %>

            </tr>
    </table>
</body>

</html>